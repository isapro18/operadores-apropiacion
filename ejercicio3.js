// Inicializa el contador de visitas con un valor de 100
let contador = 100;
// Suma 25 visitas al contador (visitas nuevas recibidas)
contador += 25;
// Resta 10 visitas del contador (por error de registro)
contador -= 10;
// Suma 5 visitas adicionales al contador (visitas nuevas registradas)
contador += 5;
// Muestra el valor final del contador en una alerta
alert(`El contador final de visitas es: ${contador}`);